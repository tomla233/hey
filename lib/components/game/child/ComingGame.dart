//即将上线
import 'package:flutter/material.dart';

class ComingGame extends StatelessWidget {
  const ComingGame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
